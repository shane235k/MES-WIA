# 1. so besides creating a work order manually we can also prompt gemini to create a workflow with specifications. for that you have to write an entire system if the user misses some info it should ask the user for the info and optional info that wont affect much like names it should tell the user a valid batch id or name it gives and ask if the user wishes to proceed with it and if not provide his own preferences this will be for optional fields, when AI has everything it needs, it should give us a json with all necessary fields we require then get it in a controller that cna convert that json into a work_order afrer that is done display the multi phase work_order creation div with all the specificcations of the work_order provided by the user and converted and returned via gemini 